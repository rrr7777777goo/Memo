@mixin : 함수와 비슷한 역할을 하는 문법

@mixin ellipsis {
	text-overflow: ellipsis;
	white-space: nowrap;
	overflow: hidden;
}

p {
	width: 300px;
	@include ellipsis;
}

@mixin은 변수도 받을수 있다!

@mixin abc ($a, $b, $c: 넣고싶은 기본값)
